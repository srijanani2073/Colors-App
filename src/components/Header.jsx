import React from 'react';
import '../styles.css';

const Header = () => (
  <header className="header">
    <div className="logo">🎨</div>
    <h1 className="title">Let's Learn Colors!</h1>
    <div className="logo">🌈</div>
  </header>
);

export default Header;
